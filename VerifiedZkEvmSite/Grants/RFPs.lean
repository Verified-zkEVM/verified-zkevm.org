import VersoBlog

open Verso Genre Blog

#doc (Page) "Requests for Proposals" =>

<!-- The current site has an empty RFP body beyond the heading. This page preserves that state while keeping a stable URL for future additions. -->

The current site does not list active RFP text beyond the section heading itself.

This page is kept as a stable location for future requests for proposals so that grant calls can be linked directly without changing the rest of the site structure.
